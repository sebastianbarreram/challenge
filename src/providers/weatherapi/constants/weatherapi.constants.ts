import serviceConfiguration from '../../../config/service-configuration';

export const WEATHER_API_BASE_URL = serviceConfiguration().weather_api.base_url;
export const WEATHER_API_KEY = serviceConfiguration().weather_api.key;
